<ul id='links'>
  <% @links.each do |link| %>
    <!-- <li>
      Title : <%= link.title %>
      Url: <%= link.url %>
    </li> -->
    <li><a href="<%=link.url%>"><%=link.title%></a></li>
  <% end %>
</ul>

<form action='/links/new'>
<input type=submit value='Add new link'>
</form>
