<ul id='links'>
  <% @links.each do |link| %>
    <li><a href="<%=link.url%>"><%=link.title%></a> Tags: <%= link.tag %></li>
  <% end %>
</ul>

<form action='/links/new'>
<input type=submit value='Add new link'>
</form>
