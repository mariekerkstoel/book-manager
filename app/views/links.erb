<ul id='links'>
  <% @links.each do |link| %>
    <li><a href="<%=link.url%>"><%=link.title%></a> Tags: <%= link.tags.map(&:name).join(', ') %></li>
  <% end %>
</ul>

<form action='/links/new'>
  <input type=submit value='Add new link'>
</form>
